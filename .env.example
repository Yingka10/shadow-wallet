# 複製成 .env.local（已被 .gitignore 擋住）後填入你自己的值。
# 這個檔案只放變數名稱與 placeholder，不放任何真實網址、金鑰或 project ref。

# development | staging | production | test
# 沒有設定時 App 會拒絕啟動 —— 不會自動採用 production。
EXPO_PUBLIC_APP_ENV=

# 專案「根」URL，例如 https://<ref>.supabase.co
# 不要填 /rest/v1、/auth/v1 這類 endpoint，supabase-js 會自己接上。
EXPO_PUBLIC_SUPABASE_URL=

EXPO_PUBLIC_SUPABASE_ANON_KEY=

# 你預期連到的 project ref。development 與 staging 必填，
# 與上面 URL 解析出來的 ref 不一致時 App 會停止連線。
EXPO_PUBLIC_EXPECTED_SUPABASE_PROJECT_REF=

# show | hide | auto（預設 auto）
# hide 只關掉畫面上的標示，環境驗證照跑。
EXPO_PUBLIC_ENV_BADGE_MODE=

EXPO_PUBLIC_GEMINI_API_KEY=

# off | fake | live（沒設定時一律 off）
# 任務 AI 建議要不要接上真的 Edge Function。
#   off   完全不呼叫，畫面上不出現 AI 區塊
#   fake  本機假資料（只有 development / staging 允許）
#   live  真的呼叫 task-ai-recommendation，會消耗模型配額
# production 不接受 fake —— 對真實家庭顯示寫死的建議比沒有這個功能更糟。
# live 失敗不會退回 fake，只會顯示「目前無法取得建議」。
EXPO_PUBLIC_TASK_AI_MODE=

# off | live（沒設定時一律 off；fake 一律降成 off，見 childGoalPlanningClient.ts）
# 孩子端「一起想怎麼開始」（P1 Child Goal Planning）要不要接上真的 ai-proxy。
# 自己的開關，不與上面 TASK_AI_MODE 共用 —— 打開任務建議不該順手打開這條
# 還在驗證中的路徑。關著時孩子端走 legacy 兩步送出，一個字都不會變。
EXPO_PUBLIC_CHILD_GOAL_PLANNING_AI_MODE=

# off | live（沒設定時一律 off；fake 一律降成 off，見 planDraftClient.ts）
# 孩子提案的草稿整理（P0-3）與 P1 正式計畫送出時的政策 enrichment
# （分類／完成標準／幣值參考價）共用這一個開關 —— 兩者都是同一條
# childProposalPlanDraft 鏈。關著時 P1 正式計畫仍然送得出去，只是
# policy_session_coin_reference / policy_payout_type 留空，
# 缺的項目會列進 requiresParentDecision。
EXPO_PUBLIC_CHILD_PROPOSAL_AI_MODE=
