# GrowBook(Shadow Wallet)系統深度體檢 — AUDIT 2026-07

> 審計基準:程式碼現狀(工作樹 @ 2026-07-04,HEAD `65eff27`,含未提交變更)。
> MASTER.md(2026-05-30)作為「意圖聲明」對照組,非事實來源。
> 每條發現附 檔案:行號;信心分級 [高/中/低];⚠️ = 推測未驗證。
> Supabase live DB 尚未接通(MCP 待授權)— 凡標「LIVE?」者需 live 環境補驗。

---

## 第 0 章 · 審計範圍與已知落差

**事實**:MASTER.md 最後更新 2026-05-30,git 之後至少 30+ commits(至 `65eff27`),工作樹另有未提交變更(`ParentSettingsScreen.tsx`、`ParentHomeTablet.tsx` 修改中;`useFamilyChildSummaries.ts` 未追蹤)。

MASTER.md 已確認過時的敘述(信心 [高]):

| MASTER 敘述 | 程式碼現狀 |
|---|---|
| §十五「平板管理頁未建」 | `src/screens/parent/tablet/ParentManageTablet.tsx` 存在且有 1000+ 行 |
| §十五「三分頁導航容器未建」 | `ParentTabNavigator.tsx:56-64,278-281` 以 `width >= 768` 自適應分流,三分頁已運作 |
| §五「缺口三:complete-task 非原子」 | `supabase/migrations/20260615000002_fn_complete_task.sql` 原子 RPC 已建,前端走 RPC(`taskActions.ts:308`) |
| §十五「首頁右欄兌換/提案靜態佔位」 | `ParentHomeTablet.tsx:2297` 起,右欄接 `useParentRedemption` 真資料,有同意/拒絕按鈕(2410-2424) |
| §十五「月報/紀錄 tab 靜態佔位」 | 月報 tab 已接 `useParentMonthlyReport`(`ParentWeeklyTablet.tsx:3,325`);紀錄 tab 仍是佔位(見 1-D) |

**判斷 [高]**:文件-程式碼落差是系統性的。CLAUDE.md 的 schema 清單同樣過時(缺 `child_tasks`、`growth_moments`、`parent_observations`、`system_task_templates`、`redemption_requests` 五張實際使用中的表,見 1-C)。CLAUDE.md「目前進度」區塊說棄坑偵測「lib/abandonDetection.ts 尚未建立」,實際上該功能以 Edge Function 存在(`supabase/functions/detect-abandonment/`)。

**用戶已確認的審計參數**:剩餘時間 6 週以上(裁決從寬);demo = 實體平板(家長端)+ 手機實機(孩子端)+ 錄影備援;刻意 defer 僅「家長端手機版」;月報、諮詢 Agent、非 6-9 歲介面正常審。

---

## 第 1 章 · 系統盤點(Phase 1,事實層)

### 1-A 導航可達性地圖

入口:`App.tsx:113`(initialRoute=`Entry`)。單一 Stack + ParentTab(BottomTabs)。

**家長端依裝置寬度分流**(`ParentTabNavigator.tsx:56-64`):手機 <768 → 四分頁(面板/任務/兌換/週報,手機版畫面);平板 ≥768 → 三分頁(首頁/週報/管理,平板畫面)。**協議點名問題的答案:家長手機版不是孤島 — 它是手機寬度下的唯一 UI,入口 = 手機開 App 登入家長帳號**(信心 [高])。

注意:5 個 tab route 無論寬度都有註冊(`ParentTabNavigator.tsx:296-300`),平板上 Tasks/Redemption 沒有 tab 按鈕但可被程式導航到(`ParentWeeklyReportScreen.tsx:423` 有 `navigate('Tasks')`)。

#### 完整可達性表(依裝置)

| 畫面 | 手機 | 平板 | 入口證據 |
|---|---|---|---|
| Entry / ParentLogin / ChildLogin | ✅ | ✅ | `App.tsx:117-119`;Entry 有 session 自動跳轉(`EntryScreen.tsx:19`) |
| Onboarding→GoalSetup→TaskSelection→Overview | ✅ | ✅ | `EntryScreen.tsx:68`、`ParentLoginScreen.tsx:120`、鏈:`OnboardingScreen.tsx:179`→`GoalSetupScreen.tsx:98`→`TaskSelectionScreen.tsx:133` |
| 孩子端 Home/Wallet/Wish/Profile | ✅ | ✅ | `ChildLoginScreen.tsx:145`;BottomNav 互通(`HomeScreen.tsx:334-350`) |
| LongTermDetail | ✅ | ✅ | `HomeScreen.tsx:258` |
| ParentHomeTablet / ParentWeeklyTablet / ParentManageTablet | — | ✅ | tab 分流 |
| 手機版 Dashboard/TaskList/Redemption/WeeklyReport | ✅ | ❌(無 tab 鈕) | tab 分流 |
| **ParentSettings** | ✅(4 處入口) | **❌ 不可達** | 入口全在手機版畫面:`ParentDashboardScreen.tsx:388`、`ParentTaskListScreen.tsx:320`、`ParentRedemptionScreen.tsx:1327`、`ParentWeeklyReportScreen.tsx:481`;平板三畫面 grep 無 `ParentSettings`(0 hits) |
| **AddChild** | ✅(經 Settings:354) | **❌ 不可達(未提交變更後)** | 唯一入口 `ParentSettingsScreen.tsx:354`;未提交 diff 移除了平板側欄「＋新增孩子」鈕(`git diff ParentHomeTablet.tsx` 刪除 `onAddChild`/`navigate('AddChild')`) |
| ParentTaskCreate | ✅ | ❌(由 NewTaskPanel 內嵌取代) | `ParentDashboardScreen.tsx:424`、`ParentTaskListScreen.tsx:442` |
| ParentTaskDetail / ObservationHistory | ✅ | **❌ 不可達** | 入口全在手機版:`ParentDashboardScreen.tsx:436`、`ParentTaskListScreen.tsx:357,431`、`ParentWeeklyReportScreen.tsx:429`;ObservationHistory 唯一入口 `ParentTaskDetailScreen.tsx:1180` |
| ParentTaskEdit | **❌ 不可達** | ✅ | 唯一入口 `ParentManageTablet.tsx:919` |
| ParentLongTermCreate | ✅ | ❌ | 唯一入口 `ParentTaskListScreen.tsx:332`(平板用 ParentHomeTablet 內嵌長期任務建立面板,`ParentHomeTablet.tsx:1695-2247`) |

**初步判斷(Phase 3 深究)**:
- [高] **平板端(=demo 裝置)到不了 Settings 與 AddChild**。未提交變更把 AddChild 入口從平板側欄移到 ParentSettingsScreen(`git diff`:`+onPress={() => navigation.navigate('AddChild')}`),但 ParentSettingsScreen 本身平板不可達 → 若此變更提交,平板將無法新增第二個孩子。改動中的 regression。
- [高] ParentTaskDetail(含觀察記錄功能)只活在手機版 — 平板端點任務沒有詳情頁。
- [中] 殼畫面:`LongTermDetailScreen.tsx:605-609` — goal_type 為 skill/challenge 時顯示「此類型長期目標詳情即將推出」(habit、family 有完整視圖);`ParentWeeklyTablet.tsx:291-293,884` 紀錄 tab「即將推出」。

### 1-B Edge Function 呼叫審計

| Function | 前端呼叫點 | 判定(信心) |
|---|---|---|
| ai-proxy | `taskRecommend.ts:52`、`aiAgent.ts:5`(classifyTask/screenRedemptionRequest/suggestTaskCoin/suggestRewardCoin 共用)、`ParentHomeTablet.tsx:837`(**繞過 aiAgent lib 直接 invoke**) | 使用中 [高] |
| **complete-task** | **零呼叫點**(grep `functions.invoke` 全 src) | **孤兒** [高]。內部只是包 `complete_task` RPC(`functions/complete-task/index.ts:62`),前端已直接呼叫同一 RPC(`taskActions.ts:308`) |
| detect-abandonment | 零前端呼叫;檔頭宣稱 cron 每日 21:00(`index.ts:4`) | **依賴 cron,repo 無任何 cron 設定證據**(config.toml 只有 verify_jwt;migrations 無 pg_cron)。LIVE? [中] |
| generate-degradation-suggestion | `aiAgent.ts:47`,傳 `{taskName, age, days}` = lightweight mode | 使用中;但 rich mode 的 intervention_log 寫入(`index.ts:141`)**永不觸發**,因呼叫端只用 lightweight [高] |
| generate-weekly-report | `useParentWeeklyReport.ts:397`(前端手動觸發)+ 檔頭宣稱週日 cron | 使用中(手動);cron LIVE? [中] |
| settle-weekly-interest | 零前端呼叫;內部呼叫 RPC `settle_weekly_interest`(`index.ts:19`) | 依賴 cron,LIVE?;**該 RPC 不在 migrations** [高] |

**RPC 盤點**:

| RPC | migration | 呼叫點 |
|---|---|---|
| complete_task | ✅ `20260615000002` | `taskActions.ts:308` |
| redeem_wish | ✅ `20260615000001` | `WishScreen.tsx:265` |
| setup_child_tasks | **❌ 無 migration(只存在 live DB)** | `taskRecommend.ts:71` |
| my_parent_id | **❌ 無 migration** | `taskActions.ts:38` |
| settle_weekly_interest | **❌ 無 migration** | Edge Fn 內部 |
| my_family_id | **❌ 無 migration**(intervention_log RLS 依賴它,`20260530000000:135-141` 使用) | RLS 政策 |

**判斷 [高]**:repo 的 migrations 不完整 — 核心表(families/tasks/wallets…)與至少 4 個 DB 函數只存在 live DB。任何人 clone repo 無法重建資料庫;這在答辯「可重現性」上是弱點,也讓 schema 審計必須以 live DB 補驗(LIVE?)。

### 1-C Schema 使用地圖

types/database.ts 定義 22 張表。按實際使用分層:

**前端+後端都在用(健康)**:families, parents, children, child_profiles(onboarding 寫入 `onboarding.ts:180-295`)、tasks, child_tasks, task_completions, wallets, transactions(taskActions/useWallet/WalletScreen)、long_term_goals(taskActions 四型建立 + 5 個 hook 讀)、reward_items + redemption_requests(WishScreen 寫、useParentRedemption 讀寫)、weekly_reports(Edge Fn 寫 `generate-weekly-report/index.ts:231`、ProfileScreen:114 與 useParentWeeklyReport:283 讀)、monthly_reports(`useParentMonthlyReport.ts:202,269`)、growth_moments(`useParentWeeklyReport.ts:275,386` 讀寫)、parent_observations(`ParentTaskDetailScreen.tsx:787,877`、ObservationHistory:84)、system_task_templates(`taskRecommend.ts:9`)、overrides(`taskActions.ts:85`)、time_savings(寫:RPC `fn_complete_task.sql:108`;讀:`useParentDashboard.ts:142`、`ProfileScreen.tsx:104`)。

**定義了、程式碼零使用**(信心 [高],全 repo grep):
- `credit_logs`(types/database.ts:645)— 零讀寫
- `sibling_relations`(types/database.ts:658)— 零讀寫

**寫入路徑全部休眠**:
- `intervention_log` — 表定義完整(append-only RLS、context_snapshot、ai_suggested/parent_decision 欄位,`20260530000000`)。僅有的兩個寫入端:detect-abandonment(無人觸發,1-B)與 degradation-suggestion rich mode(呼叫端只用 lightweight,1-B)。前端零讀取。**「可追溯/audit log」目前是死路**(H6 證實)[高]。

**欄位級孤兒(long_term_goals,`20260523000000` 一次加入)**:`role_title`、`salary_mode`、`base_salary`、`weekly_target_rate`、`privilege_reward` — 除 types 定義外全 src 零使用(grep 0 hits)[高]。`level_definitions`/`interrupt_count`/`min_age` 有使用(`taskActions.ts:608,612` 等)。`target_value/current_value/value_unit`(challenge 型)被 4 個 hook 讀取,但 **未發現任何寫入 current_value 的路徑** — challenge 型進度可能永遠是 0(⚠️ 待 Phase 2 確認)[中]。

### 1-D 殘留物清單

| 項目 | 位置 | 性質 |
|---|---|---|
| 設計原型兩套(含 20+ jsx/html/png/中文檔名 HTML) | `src/_design-ref/`、`src/ipad_design-ref/` | 非 runtime(無 import),但住在 src/ 內;`ipad_design-ref/project/screenshots/` 有 17 張除錯截圖 |
| 孩子端設計原型 HTML ×3 | `child-pattern-design/` | 非 runtime |
| web 殼 | `web/index.html`、`constants/webStyles.ts`(HomeScreen:31、ProfileScreen:17 有 import) | webStyles 是活代碼 |
| scratch 目錄 | `scratch/` | 未盤點內容 |
| 舊分析文件 ×3(5 月) | `GAP_ANALYSIS.md`、`DATA_SOURCE_ANALYSIS.md`、`docs/backend-analysis-2026-05-21.md` | ⚠️ 未逐一比對,推測部分過時 |
| 孤兒 Edge Function | `supabase/functions/complete-task/` | 見 1-B |
| 向後相容 stub | `aiAgent.ts:141-145` `generateWeeklyInsight` 回傳空字串 | 自我標註 stub |
| 死分支 | `HomeScreen.tsx:347-349`、`ProfileScreen.tsx:428`「功能開發中」alert(所有 tab 都已處理,分支不可達) | 無害殘留 |
| 「即將推出」殼 | `LongTermDetailScreen.tsx:606-609`(skill/challenge)、`ParentWeeklyTablet.tsx:291-293`(紀錄 tab) | 見 1-A |

---

## 第 2 章 · 黃金路徑追蹤(Phase 2)

路徑:onboarding → 建孩子 → 指派任務 → 孩子看到 → 完成回報 → 發幣 → 許願/兌換 → 週報。
標記:✅ 通 / ⚠️ 繞路或有缺陷 / ❌ 斷。

### 2-1 家長 Onboarding ✅(有兩處缺陷)

`EntryScreen:68`「第一次使用」→ `OnboardingScreen`(signUp + 6 題 Baumrind 問卷 + 孩子資料)→ `submitOnboarding`(`onboarding.ts:237-299`:families→parents→children→child_profiles→wallets 五段順序 insert)→ `GoalSetupScreen`(AI 建議幣值 `suggestCoinWithAI`,`taskRecommend.ts:49-57`)→ `TaskSelectionScreen`(system_task_templates + 自訂)→ `OverviewScreen` → `confirmSetup` → **RPC `setup_child_tasks`**(`taskRecommend.ts:71-78`,JSDoc 宣稱原子;**原始碼不在 repo,LIVE?**)→ reset 到 ParentTab(`OverviewScreen:85-86`)。

缺陷:
1. [高] **childPin 被丟棄**:`OnboardingInput` 收 `childPin`(`onboarding.ts:220`),但 `submitOnboarding:238` 解構時不取,children insert(267-277)無 `pin_code`。對照 `addChildToFamily:187` 有寫入。後果:onboarding 設的 PIN 無效。**有補救**:孩子首次登入若無 PIN 會進設定流程(`ChildLoginScreen.tsx:127-133`),所以是「體驗矛盾」非阻斷 — 家長剛設完 PIN,孩子登入卻被要求再設一次。
2. [中] `submitOnboarding` 五段 insert 無 transaction:中途失敗留半套資料(family 已建、parent 失敗 → 重試再建一個 family)。與系統其他地方(complete_task RPC 的原子化)標準不一致。

### 2-2 建立第二個孩子 ⚠️(平板斷)

鏈路 `AddChildScreen:99` → `addChildToFamily`(健康)→ GoalSetup 起的 onboarding 後半。但如 1-A:平板唯一入口在未提交變更中被移除,ParentSettings 平板不可達 → **demo 裝置(平板)無法新增孩子**。

### 2-3 指派任務 ✅

平板:NewTaskPanel 內嵌於 ParentHomeTablet — 一般任務(`:807-884`,AI 建議幣值直接 invoke ai-proxy `:837`)寫 tasks+child_tasks(`:1284-1313`);長期任務三型面板(habit `:1695`、skill `:1915`、family `:2105`)走 `taskActions.ts` 的 create*Goal(寫 tasks+long_term_goals+child_tasks)。手機:ParentTaskCreateScreen(`:223-264`)。兩條路都通。

### 2-4 孩子端看到任務 ✅(有系統性時區 bug)

`useTodayTasks` 邏輯完整:child_tasks→tasks→今日 completions→long_term_goals,前置解鎖判定(`:119-125`)、day_type/recurrence_days/due_date 過濾(`:134-152`)、Realtime 訂閱 task_completions(`:203-219`)。

[高] **日期邊界 bug(系統性)**:`useTodayTasks.ts:87-88` 用 `'YYYY-MM-DD'` 字串直接與 timestamptz 比較 — DB 以 UTC 解讀字串,實際查詢窗口是台北 08:00–次日 08:00。`completed_at` 存的是 +08:00 時間戳(`taskActions.ts:306`),**台北 00:00–08:00 的完成會被歸到前一天**。而 RPC 的重複防護用正確的 `AT TIME ZONE 'Asia/Taipei'`(`fn_complete_task.sql:76-77`)→ 早上 8 點前:UI 顯示未完成,點擊回報卻彈「今天已經完成過這個任務了」(`taskActions.ts:327`)。同 pattern 至少還出現在:`applyHabitResume`(`taskActions.ts:371-372`)、`useParentDashboard.ts:147-148`。**morning demo 高風險**。

### 2-5 完成回報 → 發幣入帳 ✅(本系統最健康的一段)

TaskCompleteModal → `completeTask`(`taskActions.ts:293-336`)→ RPC `complete_task`(`20260615000002`):重複防護、發幣、transactions、time_savings(Task-B)、habit 里程碑全在同一 transaction;加幣用 `balance = balance + ?` 併發安全;錢包經 Realtime 同步。曾經的「缺口三」確實已修 [高]。

[中] 安全缺陷:`complete_task` 與 `redeem_wish` 都是 `SECURITY DEFINER` 且**函數內無呼叫者授權檢查**(不驗證 auth.uid() 與 p_child_id 的家庭歸屬)— 任何登入用戶可替任意孩子完成任務/兌換。單一家庭 demo 無感,答辯被問到 RLS 時是漏洞(RLS 被 SECURITY DEFINER 繞過)。

### 2-6 許願 → 家長核可 → 兌換 ❌ **斷(P0)**

逐步證據:
1. 孩子許願:`WishScreen.tsx:316-325` insert reward_items,**`coin_cost: 0`**、`parent_approved: false`。無 MASTER §八宣稱的三步問答與 AI 建議幣值 — WishModal 只收一段文字。
2. 家長核可:平板右欄 `WishApprovalCard onApprove={approveChildWish}`(`ParentHomeTablet.tsx:2651`);手機 `handleConfirmWish`(`ParentRedemptionScreen.tsx:1287-1291`)。兩端都呼叫 `approveChildWish`(`useParentRedemption.ts:265-271`)— **只設 `parent_approved: true`,全 codebase 無任何地方為孩子願望設定幣值**。
3. 孩子兌換:兌換鈕條件 `parent_approved && coin_cost > 0 && balance >= coin_cost`(`WishScreen.tsx:345`)→ **coin_cost 恆為 0,兌換鈕永不出現**。
4. 矛盾文案:核可後家長端顯示「已上架 ·『X』0 幣,孩子可以前往撲滿兌換」(`ParentHomeTablet.tsx:2373`);孩子端幣值顯示「?」(`WishScreen.tsx:368`)。

**demo 當場出錯**:只要現場演「孩子許願→家長同意→孩子兌換」,流程在第 3 步死掉,且第 2 步就顯示矛盾數字。

支線狀態:
- ✅ 家長提案獎勵:`addParentProposal`(`useParentRedemption.ts:277-292`)有定價、直接 approved → 孩子可兌換(`redeem_wish` RPC 原子、冪等、防併發,`20260615000001`)。**目前唯一走得通的兌換路**。
- ✅ Onboarding 兌換目標:經 `setup_child_tasks` 寫入(LIVE? 未驗證)。
- ❌ `redemption_requests` + AI 篩選管線(`enrichPendingWithAi`,`useParentRedemption.ts:302-330`)是**無水之渠**:唯一的 insert 被註解在 `WishScreen.tsx:231-251`(自我標註「預留:改為家長審核流程時取消這段註解」)。AI 審核 UI(`RedemptionProposalCard`)永遠空白。MASTER §四 WF-08 宣稱的兌換審核流,實際是「上架審核」而非「兌換審核」。

### 2-7 週報生成 ⚠️(手動路通、自動路未驗證、降級半套)

- 觸發:平板週報頁「生成報告」鈕 → `requestAiRefresh`(`useParentWeeklyReport.ts:395-402`)→ Edge Fn(HTTP mode 單孩子)。cron mode 程式存在(`generate-weekly-report/index.ts:297-307`)但排程本身無 repo 證據(LIVE?,1-B)。
- 生成:`generateInsight`(`index.ts:84-127`)直接 fetch Gemini 2.0 flash,**無 fallback — Gemini 失敗/JSON parse 失敗即 throw**,MASTER §三「可降級:AI 失敗仍輸出 deterministic 指標」在此不成立。前端的降級是:週報數字由 hook 自行查(與 AI 無關),AI 欄位顯示「尚未生成」佔位(`useParentWeeklyReport.ts:360-365`)。實際 demo 斷網按「生成報告」的 UI 行為待驗證(⚠️ 未實測)。
- 缺口一(週報覆蓋 abandonment_tier):**已修** — upsert 前先讀舊 task_adjustments 展開保留(`index.ts:226-244`)。MASTER §五「優先修」的描述過時。
- [高] 但 `abandonment_tier` **全前端零讀取**(grep 僅 hook 內部欄位無此 key)→ 就算 detect-abandonment 有 cron 在跑,棄坑警示不會出現在任何畫面;CLAUDE.md 宣稱的「第一層:孩子端溫和詢問卡」全 src 無實作。棄坑偵測是「後端半條、前端零條」。
- [中] 週界線用 UTC ISO week(`index.ts:129-137,145-148`)而非台北時區 — 與 2-4 同族的時區不一致。

### 2-8 LLM 依賴點與降級現況總表

| 環節 | LLM 呼叫 | 失敗時 |
|---|---|---|
| 任務分類/幣值建議/獎勵定價建議 | ai-proxy(5 個 handler) | `aiAgent.ts` 四個函數都有寫死 fallback ✅;但 `ParentHomeTablet.tsx:837,1180` 繞過 aiAgent 直接 invoke(fallback 待查,Phase 3) |
| 棄坑降級話術 | generate-degradation-suggestion | 前端 fallback 模板字串 ✅(`aiAgent.ts:45`) |
| 週報洞察 | generate-weekly-report 內 Gemini | ❌ Edge Fn 無 fallback;前端顯示「尚未生成」佔位 |
| 許願三步問答(MASTER §八) | — | **未實作**(WishModal 純文字輸入) |

### 2-9 demo 當場出錯點彙總(P0 候選)

1. 許願→核可→兌換在第 3 步斷 + 第 2 步矛盾文案(2-6)。
2. 首頁長期目標進度條:`useParentDashboard.ts:193-199` `current=餘額, target=coin_cost`,無達標處理 → 109/70(MASTER 開放問題 #12 仍在)。且目標取「最早建立的 active reward_item」(`:143` `order('created_at').limit(1)`)— 家長一旦提案過獎勵,首頁「長期目標」可能顯示錯的項目 [高]。
3. 早上 8 點前 demo:完成任務 UI 不更新 + 重複點擊報錯(2-4)。
4. 平板無法進 Settings / 新增孩子(2-2)。
5. 「生成報告」在 Gemini 掛掉時的錯誤處理未驗證(2-7)。
6. 測試資料髒(MASTER §十七自承:同名任務重複、分類亂)⚠️ 未驗證(需 live DB)。

---

## 第 3 章 · 逐功能價值審計(Phase 3,核心)

深度標記:完整 / 半殘 / 殼 / 孤島。裁決:保留 / 深化 / 重做 / 砍(「砍」以完整專題為標準,取捨由團隊依剩餘時間決定;時間 >6 週,裁決從寬)。

### 3-1 價值錨點總檢(先講結論)

| 錨點 | 結論 | 證據 |
|---|---|---|
| AI 不直接執行金流 | ✅ 成立 | 全部 AI 呼叫只回建議值;唯一 AI 寫 DB 是 `ai_verdict` 欄位(`useParentRedemption.ts:316-319`),非金流 |
| AI 提議、家長拍板 | ✅ 成立(在有接的流程裡) | suggestTaskCoin 只填輸入框預設值,送出仍家長按 |
| **金流變動必經家長** | ❌ **違規(非 AI,是孩子)** | 見 3-2 #4:孩子自建 C 類任務,自選難度與時間,完成即發幣,全程無家長參與 |
| 成長幣 vs 時間儲蓄語義分離 | ⚠️ 結構成立但空洞 | time_savings 無任何兌換路徑(`is_redeemed` 全 codebase 零寫入)→「不可兌換成物品」是因為「什麼都不能兌換」 |
| 只記錄不誘導、無成癮機制 | ✅ 成立 | 無推播、無連續登入獎勵;habit 中斷是「退一天、保底 checkpoint」的反挫折設計(`taskActions.ts:352-`)反而加分 |

### 3-2 孩子端功能

| # | 功能 | 意義(SDT/動機) | 深度 | 兌現(證據) | 偏離 | 裁決 |
|---|---|---|---|---|---|---|
| 1 | 今日任務清單 | 勝任感:當日可完成的清單 | 完整 | `useTodayTasks` 全鏈:分類排序、day_type/recurrence/due 過濾、Realtime(見 2-4) | 時區 bug(2-4) | **保留**,修 tz |
| 2 | 完成回報+動畫 | 即時回饋(勝任感) | 完整 | RPC 原子(2-5)+ FeedbackAnimation | 無 | **保留**(健康) |
| 3 | 前置解鎖打折 | 推力:不隱藏、打折+說明原因 | 完整 | 後端 `fn_complete_task.sql:58`(×0.7);前端 `TaskItem.tsx:13-16,83,111`「打折中 🔓」;**有測試** `ContributionTaskCard.test.tsx:34-46` | 無 | **保留**(答辯亮點:行為科學落到 code) |
| 4 | **孩子自建任務** | 自主感(原意應是 WF-06 提案) | 完整但**錯的完整** | `HomeScreen.tsx:85-87,404-458`:孩子自選 B/C 類、自填分鐘、C 類自選難度 1-3;`createChildTask`(`taskActions.ts:755-810`)直接 `is_active: true` 上架,無審核、無 AI 分類、無上限 → 完成即得 `round(time×difficulty)` 幣 | **P0 價值違規:孩子可無限鑄幣**。MASTER §七 WF-06 宣稱的「AI 分類→建議幣值範圍→家長同意才上架」一項都沒接 | **重做**:插入待審狀態(`is_active:false` + 家長核可),AI 分類為第二道防線;6 週夠做 |
| 5 | 錢包(雙帳戶) | 勝任感+9-12 規劃力 | 完整 | `useWallet`(spending+saving+Realtime);WalletScreen 交易紀錄(`:158-188`) | 無 | **保留** |
| 6 | 許願樹 | 自主感:孩子表達願望 | 半殘 | 斷鏈證據見 2-6;MASTER §八三步問答+AI 建議幣值未做(WishModal 純文字,`WishModalComponent.tsx:308`) | 斷鏈本身即偏離「第一次兌換成功是留存關鍵」(MASTER §四 WF-03) | **重做定價環節**(S 級工作量:核可時彈定價框);三步問答另議 |
| 7 | 長期任務 habit | 勝任感:連續打卡+里程碑 | 完整 | 建立面板+`createHabitGoal`;RPC 里程碑發幣(`fn_complete_task.sql:119-148`);soft-reset 反挫折(`applyHabitResume`) | 無 | **保留**(亮點) |
| 8 | 長期任務 skill | 勝任感:階段里程碑 | 半殘 | 建立面板+`createSkillGoal`(`taskActions.ts:550-612`,level_definitions)有;**孩子端詳情=殼**(`LongTermDetailScreen.tsx:605-609`「即將推出」) | 無 | **深化**(補孩子端視圖)或暫藏建立入口 |
| 9 | 長期任務 family(連結感) | **全系統唯一 SDT 連結感載體** | 半殘 | 有:建立(`createFamilyGoal:668-736`)、打卡(`FamilyRoleView:313-332`)、時間累積(RPC Task-B 路徑)、進度%。缺:時間存摺**回饋迴路**(無兌換成家庭時間的機制)、角色認同(role_title 欄位孤兒,1-C)、期末儀式(到期只剩 daysLeft=0,無任何流程) | 見 3-4 結構性缺口 | **深化**(結構性優先) |
| 10 | 長期任務 challenge | — | 殼 | 型別+4 個 hook 讀取(`useLongTermTasks.ts:68-70` 等),**無建立入口、無寫 current_value 的路徑**(1-C) | 設計未兌現 | **砍**(從型別與 hook 移除)或明確 defer |
| 11 | Profile | 自我觀察 | 完整 | 讀 wallets/time_savings/weekly_reports/completions(`ProfileScreen.tsx:87-152`) | 無 | **保留** |
| 12 | 棄坑詢問卡(CLAUDE.md 宣稱三層) | 反挫折介入 | **缺席** | 前端 grep `abandonment` 0 hits;後端 detect-abandonment 無觸發者(1-B);tier 寫入 weekly_reports 後無人讀(2-7) | 文件宣稱 vs 實作:三層全缺 UI | **深化**(見 3-4)或從宣稱移除 |

### 3-3 家長端(平板=demo 目標)

| # | 功能 | 深度 | 兌現(證據) | 裁決 |
|---|---|---|---|---|
| 13 | 首頁(側欄/總覽/今日任務) | 完整(改造中) | 未提交 diff 正把側欄升級為全孩子摘要(useFamilyChildSummaries) | **保留**;修 109/70(2-9 #2)與目標選取邏輯(`useParentDashboard.ts:143`) |
| 14 | MarkPanel(WF-05 Override) | 半殘(功能通、工程債) | `parentMarkTask`(`taskActions.ts:28-150`):5 段循序寫入**非原子**;餘額 read-then-write 無條件鎖(`:116-121`,正是 complete_task 修掉的 race pattern);**未寫 intervention_log** — CLAUDE.md §intervention_log 宣稱「兩表同一 transaction」不實 | **深化**:封成 `mark_task_atomic` RPC + intervention_log 同 tx(這正是 MASTER §五缺口三關聯的既定方針) |
| 15 | NewTaskPanel(一般+3 型長期) | 完整 | 2-3;AI 建議 fire-and-forget 降級佳(`ParentHomeTablet.tsx:837-844`) | **保留** |
| 16 | 右欄兌換待審 | 半殘 | 核可無定價(2-6) | **重做定價**(與 #6 同一修) |
| 17 | 右欄任務提案卡 + AI 篩選管線 | **孤島** | `RedemptionProposalCard`+`enrichPendingWithAi`+`approveRequest(adjustedCoins)` 全套完整,掛在零寫入的 redemption_requests 上(2-6) | **接水**:把孩子兌換改走申請流(取消 `WishScreen.tsx:231-251` 註解即接通大半)— 這會同時兌現 WF-08 與「AI 提議家長拍板」的最佳 demo;或砍 UI |
| 18 | 週報 tab | 完整- | 手動生成+aiReady 降級 UI;MASTER §十五自承缺每日長條圖/長期進展(hook 已有 longTermGoals `useParentWeeklyReport.ts:291,371` — 部分已補,畫面呈現 ⚠️ 未逐項驗證) | **保留** |
| 19 | 月報 tab | 完整- | 接真資料(`ParentWeeklyTablet.tsx:325`)+家長月度觀察輸入(`:420`) | **保留**(MASTER 說佔位已過時) |
| 20 | 紀錄 tab | 殼 | 「即將推出」(`ParentWeeklyTablet.tsx:291-293`) | 補(素材=overrides/intervention_log/growth_moments)或藏 tab |
| 21 | 管理頁 | 完整 | 任務庫/獎勵/兌換歷史接真資料(`ParentManageTablet.tsx:2`) | **保留**;缺「設定」區(MASTER IA 有)→ 與 #22 同修 |
| 22 | Settings / AddChild(平板) | **不可達** | 1-A;未提交變更加劇 | **P0/P1 修**:ParentManageTablet 加設定區或入口 |
| 23 | 家長端手機版 | 完整(舊 IA) | 手機寬度預設 UI(1-A) | **P3 defer**(用戶確認);注意它不是死代碼,是手機用戶的實際畫面 |

### 3-4 流程一與後端宣稱

| # | 功能 | 深度 | 兌現(證據) | 裁決 |
|---|---|---|---|---|
| 24 | 問卷+Baumrind 分型 | 完整- | 6 題雙維度計分(`onboarding.ts:24-129`)紮實;但 baumrind_type 全系統只在週報 prompt 出現一次(`generate-weekly-report/index.ts:95`)—「教養個人化」的兌現極薄 | **深化**(至少讓週報建議明顯隨型別變化)或答辯時如實定位為「已埋點」 |
| 25 | 初始任務推薦+目標設定 | 完整 | 規則推薦(`taskRecommend.ts`)+`setup_child_tasks` 原子 RPC(LIVE?) | **保留**;把 RPC 補進 migrations |
| 26 | 利息結算 WF-16 | ⚠️ 不可驗 | Edge Fn 只是殼(`settle-weekly-interest/index.ts:19` 呼叫 live-only RPC);cron 無證據;CLAUDE.md「每週 5%」vs MASTER「雙週判斷」兩文件矛盾,真相在看不見的 RPC 裡 | **補 migration + 對齊文件**(LIVE?) |
| 27 | intervention_log 可追溯 | 死路 | 1-C;寫入端全休眠 | **深化**:#14 的 RPC 化順手寫入 + 紀錄 tab(#20)當讀取端 → 一魚三吃 |
| 28 | 動機光譜 WF-11 | 殼 | motivation_level 固定寫 'external'(`onboarding.ts:196,284`),無任何更新演算法;週報 prompt 引用這個恆定值(`index.ts:94`) | 明確 defer 或砍出文件 |
| 29 | 諮詢 Agent(MASTER §十一「真的要做」) | 缺席 | 全 src 無 | 依 6 週+ 時程自行取捨;若做,#27 是其宣稱的前置 |

### 3-5 協議點名事項的裁決

- **家長手機版**:P3 defer 成立(用戶確認)。但「併入平板」不需做 — ParentTabNavigator 已是同一容器自適應,維護成本是「別碰」即可。
- **Onboarding 離完整初始化差什麼**:PIN 落地(2-1 #1)、submitOnboarding 原子化(2-1 #2)、WF-01 的 LLM 個人化未做(問卷結果只影響 Baumrind 標籤)— 前兩者 S 級,第三者可 defer。
- **家庭責任連結感機制**:**升級為結構性缺口(協議預告條件成立一半)**。不是整條缺席 — 累積迴路(承諾→打卡→時間存摺)是活的;缺的是**回收迴路**:存的家庭時間永遠花不掉(`is_redeemed` 零寫入)、無角色認同(role_title 孤兒)、承諾期滿無任何儀式(`FamilyRoleView:290-292` 只算 daysLeft)。連結感機制目前是「記帳」沒有「連結」— 孩子存了 300 分鐘家庭時間,然後呢?建議的最小完整迴路:期滿結算畫面(家長+孩子一起看達成率)+ 時間存摺兌換成「家庭活動券」(非物品,維持語義分離)。工作量 M。

---

## 第 4 章 · 四維度評估(Phase 4)

### 4-1 完整度:接近完整的系統 + 幾條沒接的線(不是功能堆疊)

骨架(每日循環:指派→顯示→完成→發幣→餘額同步)完整且工程品質高於平均(原子 RPC、Realtime、反挫折規則、前置解鎖有測試)。缺的不是散功能,是**三個閉環**:

1. **審核環**(最缺的拼圖):孩子的兩個自由輸入口 — 自建任務(3-2 #4)與許願(3-2 #6)— 都沒有經過家長閘門就進入或卡死在金流層。諷刺的是,修它的材料全在:redemption_requests 表 + AI 篩選 + 家長審核 UI + adjusted_coins 全套完整(3-3 #17),只是沒接水。**接通這一環,同時修掉 P0 價值違規、P0 斷鏈、和「AI 提議家長拍板」的最佳 demo 三件事。**
2. **追溯環**:intervention_log 設計完整、寫入端全休眠、讀取端不存在(1-C、3-4 #27)。
3. **回饋環**:連結感回收迴路(3-5)+ 棄坑三層的前端(3-2 #12)。

### 4-2 Demo 安全(demo 形式:實體平板家長端 + 手機孩子端 + 錄影備援)

**Demo 前必修**(詳見第 5 章 P0):許願定價、孩子自建任務閘門(或 demo 時不碰)、首頁 109/70、平板 Settings 入口、時區 bug(若上午 demo)。

**建議 demo 路線**:
1. 開場:平板家長端首頁(側欄多孩總覽,改造完成後)→ NewTaskPanel 指派任務含 AI 幣值建議(LLM 掛了也只是沒建議範圍,不影響流程,`ParentHomeTablet.tsx:844` catch 靜默)。
2. **雙裝置 Realtime 是現成的隱藏亮點**:孩子手機完成任務 → 家長平板即時更新(task_completions/wallets/reward_items 三個訂閱,`useTodayTasks.ts:203-219`、`useWallet.ts:52-59`、`useParentRedemption.ts:209-220`)。當場演,比講架構有說服力。
3. 孩子端:今日任務(前置解鎖打折文案)→ 完成動畫 → 錢包入帳。
4. 週報:**事先生成好**,現場只展示不點「生成報告」(該路徑無 fallback,2-7);錄影備援專門覆蓋「現場生成」片段。
5. **避開**:許願全鏈(除非 P0 已修)、紀錄 tab、skill 型詳情頁、challenge 型、家長手機版。
6. 資料準備:live DB 清髒資料(⚠️ 需 live 驗證);兩台裝置同網段;Gemini API 額度確認。

### 4-3 UI/UX 專業度

**樣式碎片化(量化)**:screens/components 硬編碼 hex 色值 **416 處**,constants(colors.ts + parentTheme.ts + webStyles.ts)才 93 處。最重:`ParentHomeTablet.tsx` 74 處 — 平板主畫面大量繞過自家 `ParentColors`。這是「有 token 系統但不守」的典型,答辯看 code 會被看穿 [高]。

**雙主題是刻意設計,可辯**:孩子端 `colors.ts` / 家長端 `parentTheme.ts` 分齡雙視覺語言成立;但同端內的一致性不足:孩子端任務卡就有 TaskItem / TaskCard / DutyTaskCard / ContributionTaskCard 四個元件並存(`src/components/`),前三者手機/舊版混用 [中]。

**分齡介面**:實質單一(6-9 一套)。age_group 在孩子端 UI 唯一的用途是 Profile 顯示 chip(`ProfileScreen.tsx:377-378`)與任務年齡欄位填值(`HomeScreen.tsx:161`)。「分齡動態參數」目前只落在幣值計算與帳戶型態(9-12 雙錢包,`onboarding.ts:266`),介面層零差異 [高]。答辯建議:誠實定位為「參數已分齡、介面 6-9 優先」,不要宣稱分齡介面。

**AI 套版感候選(⚠️ 需目測確認,以下是程式碼層訊號)**:
- 三個 auth 畫面(Entry/ParentLogin/ChildLogin)用 emoji 當圖示(`EntryScreen.tsx:50,60`),而系統其他處全是自繪 SVG(TaskIcons、ParentTabNavigator 內嵌 SVG)— 第一眼畫面反而是最粗糙的一組。精修方向:換 SVG 圖示、對齊 parentTheme 的間距語言(卡片 padding 28 → 與平板卡一致)、登入卡陰影參數統一。
- Onboarding 四畫面沿用孩子端 `Colors` 舊調色盤,與家長端後續畫面(ParentColors)斷裂 — 家長走的流程卻是孩子端視覺。精修方向:onboarding 是家長操作,改掛 parentTheme。

**加分項**:有 8 個測試檔(元件渲染 + lib 邏輯,如 `ContributionTaskCard.test.tsx` 驗打折文案)— 專題等級少見,答辯可講。

### 4-4 技術深度:文件宣稱 vs 程式碼現實

| 宣稱(MASTER §三) | 現實 | 信心 |
|---|---|---|
| 工作流驅動、LLM 單點呼叫 | ✅ 成立:6 條 Edge Fn 各自獨立,LLM 只在生成節點;ai-proxy 集中管理 API key、server 端 clamp(`ai-proxy/index.ts:86`) | 高 |
| 規則處理金流/AI 只建議 | ✅ 成立(見 3-1;唯一違規者是「孩子」不是 AI) | 高 |
| correlation_id 可追溯、能回放稽核 | ❌ **不成立**:全 repo 只有 detect-abandonment 生成 correlation_id(`detect-abandonment/index.ts:56,119`),而它沒有觸發者;無任何讀取/回放介面 | 高 |
| 可降級、AI 失敗不中斷 | ⚠️ 半套:前端四個 aiAgent 函數 + suggestCoinWithAI 呼叫端都有 fallback(`GoalSetupScreen.tsx:75-77`);**週報 Edge Fn 無 fallback**(2-7) | 高 |
| 寫失敗不中斷 | ✅ 成立於已實作處:intervention_log 寫失敗只 warn(`generate-degradation-suggestion/index.ts:159`);週報 cron 逐孩子容錯(`generate-weekly-report/index.ts:307`) | 高 |
| 金流原子性(缺口三) | ⚠️ 半套:complete_task / redeem_wish 兩條 RPC 教科書級(含防併發條件扣款);但 parentMarkTask(3-3 #14)、submitOnboarding(2-1)、FamilyRoleView 的 current_day 補寫(3-2 #9)仍是多段 JS 寫入 | 高 |
| 利息 idempotency(缺口二) | ⚠️ 不可驗:邏輯在 live-only RPC(1-B);兩份文件對利率規則的描述還互相矛盾(3-4 #26) | — |

**可深化為答辯亮點的 1-3 處**(與核心價值相關、非過度工程):
1. **接通申請-審核-AI 篩選環**(4-1 #1):現有材料接線 + 把「AI 提議、家長拍板、留痕」從口號變成可現場 demo 的流程。工作量 M,回報最大。
2. **原子 RPC 家族補齊**:parentMarkTask → `mark_task_atomic`(順手寫 intervention_log 同 transaction)→「所有金流變動都經原子 RPC + 留痕」變成完整敘事,並救活 intervention_log。工作量 S-M。
3. **雙裝置 Realtime 同步**(4-2 #2):已完成,零工作量,純 demo 編排。

---

## 第 5 章 · 綜合裁決(Phase 5)

> 回答開頭的問題:**這個系統距離「完整系統」差的不是功能量,是三個沒接上的閉環(審核/追溯/回饋)+ 一批文件與程式碼的失聯。** 骨架工程品質高於專題平均,6 週+ 的時間足夠把「接近完整」變成「完整」。

### 5-1 P0 — 阻斷(價值違規 / demo 當場出錯 / 黃金路徑斷裂)

| # | 事項 | 工作量 | 證據 |
|---|---|---|---|
| P0-1 | **孩子自建任務無閘門(可無限鑄幣)**:插入待審狀態或至少 C 類降為 B 類處理 | S(最小修:`createChildTask` 改 `is_active:false` + 家長端待審列表)/ M(接完整 WF-06) | 3-2 #4 |
| P0-2 | **許願核可無定價 → 兌換鏈斷**:家長核可時彈定價框(可掛 suggestRewardCoin AI 建議) | S | 2-6 |
| P0-3 | 首頁長期目標 109/70 + 目標選取邏輯錯誤 | S(達標顯示「可兌換!」+ 目標改取 onboarding 目標或明確標記) | 2-9 #2、`useParentDashboard.ts:143,193-199` |
| P0-4 | 平板無法進 Settings / 新增孩子(demo 裝置) | S(ParentManageTablet 加設定入口) | 1-A、2-2 |
| P0-5 | 日期邊界 bug(上午 demo 高風險;平時影響 00:00-08:00) | S-M(統一改用帶時區的 ISO 字串比較;至少修 useTodayTasks + useParentDashboard + parentMarkTask + applyHabitResume 四處) | 2-4 |

### 5-2 P1 — 完整度(答辯會被問、6 週內應完成)

| # | 事項 | 工作量 | 證據 |
|---|---|---|---|
| P1-1 | **接通申請-審核環**(redemption_requests 管線接水):孩子兌換改走申請流,救活 AI 篩選 + adjusted_coins + 右欄審核卡 | M | 4-1 #1、3-3 #17 |
| P1-2 | **parentMarkTask RPC 化 + intervention_log 同 transaction** — 救活「可追溯」宣稱 | S-M | 3-3 #14、3-4 #27 |
| P1-3 | 連結感回收迴路:family 型期滿結算儀式 + 時間存摺兌換家庭活動(結構性缺口) | M | 3-5 |
| P1-4 | 週報 Edge Fn 加 deterministic fallback(規則模板),兌現「可降級」宣稱 | S | 2-7 |
| P1-5 | onboarding:childPin 落地 + submitOnboarding 原子化(RPC 或補償) | S | 2-1 |
| P1-6 | RPC 授權檢查:complete_task / redeem_wish 內驗證 auth.uid() 家庭歸屬(RLS 答辯漏洞) | S | 2-5 |
| P1-7 | migrations 補齊 live-only 物件(setup_child_tasks / my_parent_id / my_family_id / settle_weekly_interest / 核心表)— 可重現性 | M | 1-B |
| P1-8 | cron 落地驗證(weekly report / detect-abandonment / interest)+ abandonment_tier 前端讀取(至少週報顯示警示) | M(LIVE?) | 1-B、2-7 |
| P1-9 | 文件回真:MASTER.md 與 CLAUDE.md 全面對齊程式碼(尤其 CLAUDE.md 的 intervention_log「同 transaction」不實敘述、schema 清單缺五張表) | S | 第 0 章 |

### 5-3 P2 — 專業度

| # | 事項 | 工作量 |
|---|---|---|
| P2-1 | 樣式收斂:ParentHomeTablet 74 處硬編碼色值回 token;auth 三畫面 emoji → SVG;onboarding 改掛 parentTheme(4-3) | M |
| P2-2 | 紀錄 tab 補內容(素材:overrides + growth_moments + intervention_log,與 P1-2 聯動)或先藏 | S(藏)/ M(補) |
| P2-3 | skill 型孩子端詳情頁(建立入口已在,詳情是殼) | M |
| P2-4 | 任務卡元件收斂(TaskItem/TaskCard 舊卡與 Duty/Contribution 新卡並存) | S-M |
| P2-5 | 週報畫面補 hook 已有的資料呈現(長期任務進展等,MASTER §十五缺口;部分已補 ⚠️ 需逐項目測) | S |

### 5-4 P3 — defer(附理由)

| 事項 | defer 理由 |
|---|---|
| 家長端手機版 | 團隊已決策擱置(用戶確認);平板 demo 不經過;注意仍是手機用戶預設 UI,別刪 |
| 諮詢 Agent(MASTER §十一) | 大工程;其前置(intervention_log 活水)由 P1-2 供給;6 週內做 P0+P1 優先。若行有餘力,它是最強答辯亮點 |
| 非 6-9 歲介面差異化 | MVP 邊界明確;答辯改口徑為「參數分齡、介面 6-9 優先」即可(4-3) |
| 動機光譜 WF-11 | 無資料累積基礎(motivation_level 恆為 'external');需先有行為資料再談演算法 |
| 月報敘事 WF-13 / 年齡升級 WF-15 | MASTER 原本就標未開始;不影響核心敘事 |

### 5-5 砍/重做清單

| 對象 | 裁決 | 理由 |
|---|---|---|
| challenge 型長期任務 | **砍**(型別+hook 讀取碼移除,或註明 defer) | 無建立入口、無進度寫入,純幽靈(3-2 #10) |
| complete-task Edge Function | **砍**(刪目錄) | 孤兒,前端直呼 RPC(1-B) |
| credit_logs、sibling_relations 表 + 型別 | **砍或標註 future** | 零使用(1-C) |
| long_term_goals 的 salary_mode/base_salary/weekly_target_rate/privilege_reward/role_title 欄位 | **砍(migration drop)或在 P1-3 兌現 role_title** | 欄位孤兒(1-C) |
| `aiAgent.generateWeeklyInsight` stub、HomeScreen/ProfileScreen 死分支 | **砍** | 殘留(1-D) |
| `src/_design-ref/`、`src/ipad_design-ref/`(含 17 張截圖)、`child-pattern-design/`、`scratch/` | **移出 src / 出 repo** | 設計原型混在源碼樹(1-D) |
| 孩子自建任務(現行無閘門版) | **重做**(P0-1) | 價值違規 |
| 許願定價環節 | **重做**(P0-2) | 斷鏈 |

### 5-6 答辯彈藥欄(修好之後怎麼講)

| 素材 | 答辯一句話 |
|---|---|
| 原子 RPC 家族(P1-2 完成後) | 「所有金流變動都經過 Postgres 原子函數,重複防護、條件扣款防併發、審計記錄在同一個 transaction — 孩子的獎勵不會因為網路重試而消失或翻倍。」 |
| 申請-審核-AI 篩選環(P1-1 完成後) | 「AI 會給風險評分和建議幣值,但按下同意的永遠是家長 — 這不是口號,現場可以演。」 |
| 雙裝置 Realtime(現成) | 「孩子在手機完成任務,家長平板上的餘額同一秒跳動 — 我們用 Supabase Realtime 訂閱三張表。」 |
| 前置解鎖+soft-reset(現成) | 「行為科學不是寫在企劃書裡:前置任務沒完成,C 類幣值打七折並說明原因(推力而非懲罰);習慣中斷退一天但不掉出上個檢查點(反挫折),這些都有單元測試。」 |
| 週報混合架構(P1-4 完成後) | 「數據指標由規則計算永遠可用,LLM 只負責把它翻譯成給家長的建議;Gemini 掛了,週報照出。」 |
| 為什麼不用 LangChain(MASTER §二現成) | 照 MASTER 口徑,加一句「兒童金融場景最不需要的就是 AI 自主多步決策」。 |

### 5-7 健康清單(檢查過、沒問題、不要動)

- `fn_complete_task.sql` / `fn_redeem_wish.sql`:原子性、冪等、併發安全的寫法都對(2-5、2-6 支線)。只需加授權檢查(P1-6),邏輯別動。
- `useTodayTasks` 的分類/過濾/排序邏輯與 Realtime 訂閱模式(修 tz 字串即可,結構別動)。
- `applyHabitResume` 反挫折規則、前置解鎖打折(含測試)。
- `createFamilyGoal` / `createSkillGoal` / `createHabitGoal` 的補償刪除寫法。
- ai-proxy 的集中代理 + server 端 clamp + client 端 fallback 三層結構。
- ParentTabNavigator 自適應分流(1-A)— 手機/平板一個容器,設計正確。
- onboarding 問卷計分邏輯(`onboarding.ts:99-129`)。
- 測試檔 8 個,全部保留並在 CI 跑。

### 5-8 Phase 0 假設驗證回顧

| # | 假設 | 結果 |
|---|---|---|
| H1 | 文件-程式碼落差系統性;缺口三已修文件未更 | ✅ 證實(第 0 章;缺口一也已修,2-7) |
| H2 | 三分頁容器已存在,MASTER 過時 | ✅ 證實(1-A) |
| H3 | complete-task Edge Fn 是孤兒 | ✅ 證實(1-B) |
| H4 | 兌換兩套半成品、右欄佔位 | ◐ 部分:兩套半成品證實(2-6);「右欄佔位」已過時,接了真資料但核可流程斷 |
| H5 | long_term_goals 欄位多數未兌現 | ◐ 部分:salary/privilege/role 五欄孤兒證實;level_definitions/target_value 等有使用(challenge 讀無寫) |
| H6 | intervention_log 死路 | ✅ 證實(1-C) |
| H7 | cron 無 repo 證據、demo 需手動 | ◐ 部分:repo 層證實;live 是否有 cron 待驗(LIVE?);週報前端有手動鈕 |
| H8 | monthly/credit/sibling 有 schema 無程式 | ◐ 部分:credit_logs、sibling_relations 證實;monthly_reports 已接真資料(推翻) |
| H9 | 分齡只有型別,UI 單一 | ✅ 證實(4-3) |
| H10 | 孩子端兩套樣式分支碎片化 | ◐ 修正:非兩套分支,是硬編碼繞過 token 的碎片化(4-3 量化) |
| H11 | WF 實作 ~5 條,WF-06/08 未接 | ✅ 證實(3-2 #4、3-3 #17) |

### 5-9 Live DB 驗證結果(2026-07-04 補,Supabase MCP)

原五項待驗全部完成,結果如下:

1. **cron:`cron.job` 是空的**(pg_cron extension 已安裝、零排程)[高]。週報、棄坑偵測、利息結算**從未自動執行過**。旁證:`weekly_reports` 0 列、`intervention_log` 0 列、`transactions` 中 `type='interest'` 0 列。影響:
   - MASTER §四 WF-16「✅ 已實作」實況是「函數存在、從未跑過」。
   - P1-8 升級為明確工作:建立 3 個 cron job(pg_cron + pg_net 呼叫 Edge Fn;pg_net 未安裝,需一併啟用)。
2. **live-only RPC 原始碼已取得**:
   - `settle_weekly_interest`:**缺口二證實未修,且比文件說的更糟** — 無 idempotency、無雙週判斷、無「錢沒動才計息」;每執行一次就對所有正餘額 saving 錢包直接 `balance += round(balance × interest_rate)`。利率存於 `wallets.interest_rate` 欄位(CLAUDE.md 寫死 5%、MASTER 寫雙週判斷,**兩份文件皆與實作不符**)。目前無害純粹因為 cron 沒跑 + 只有 1 個 saving 錢包。→ 併入 P1-7/P1-8:重寫此 RPC(加 `last_interest_at` 間隔判斷即可冪等)再排 cron。
   - `setup_child_tasks`:單一 plpgsql 函數 = 單一 transaction,原子性宣稱成立 ✅;寫入兌換目標 `parent_approved=true` 有定價(onboarding 目標流健康,2-6 支線證實)。
   - `my_parent_id` / `my_family_id`:如預期的 auth.uid() 查詢。
   - 四者皆 SECURITY DEFINER 無呼叫者範圍檢查(`setup_child_tasks` 可對任意 family_id 寫入)→ P1-6 範圍擴大到 RPC 全家族。
3. **RLS:22 張表全部啟用且各有 1-5 條 policy** ✅(policy 品質未逐條審,但「有開」的答辯底線成立)。建議跑一次 `get_advisors(security)` 作最後檢查。
4. **髒資料證實**:7 個 family、8 個 child、70 個 active tasks 中**同名重複任務 9 組(多出 12 列)**;`redemption_requests` 有 6 列舊測試資料(2 pending / 2 approved / 2 rejected)— 那 2 筆 pending 會讓「孤島審核 UI」在 demo 時意外顯示內容;**1 筆已核可但 coin_cost=0 的孩子願望**(2-6 斷鏈的 live 實證,孩子端現在就看得到一張「?幣、無法兌換」的卡)。→ demo 前清理清單成立。
5. intervention_log = 0 列:證實 2-7 判斷,無需修正。

*(審計完成:2026-07-04,含 live DB 驗證。全部「LIVE?」標記已解除;僅剩 ⚠️ 目測類待驗項(UI 呈現)。)*
