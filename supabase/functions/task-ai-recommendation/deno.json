{
  "//": [
    "task-ai-recommendation 專用的 Deno 設定。",
    "",
    "刻意放在 function 目錄底下而不是 supabase/functions/：",
    "上一層那份 deno.json 是所有 Edge Function 共用的，動它會改到",
    "ai-proxy / complete-task / generate-weekly-report 等五支的解析方式。",
    "這支需要 lockfile 把 esm.sh 的遠端相依釘死，其他支目前沒有 ——",
    "所以這份設定只作用在這個目錄，其他 function 一個字都沒動。",
    "",
    "不放 secret，不放 project ref。model 名稱由 GEMINI_TASK_RECOMMENDATION_MODEL",
    "環境變數提供，不寫在這裡也不寫在程式碼裡。"
  ],

  "lock": "./deno.lock",

  "compilerOptions": {
    "lib": ["deno.window"],
    "strict": true,
    "noUncheckedIndexedAccess": false
  },

  "imports": {
    "@supabase/supabase-js": "https://esm.sh/@supabase/supabase-js@2.58.0"
  },

  "tasks": {
    "check": "deno check index.ts",
    "test": "deno test --allow-read --allow-env tests"
  },

  "fmt": {
    "lineWidth": 100,
    "singleQuote": true
  }
}
