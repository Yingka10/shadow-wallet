# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts
expo-web.log

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

.env
.env.local
.env.production

# local analysis & scratch docs
DATA_SOURCE_ANALYSIS.md
GAP_ANALYSIS.md
scratch/
# docs/ 仍然整個不進 git（它多半是本機分析與 spec 草稿）。
# 寫成 docs/* 而不是 docs/ 的唯一理由：git 沒有辦法把一個被排除的**目錄**
# 底下的檔案再加回來，寫成 docs/* 才有機會用 ! 逐一放行。
docs/*
# 例外：P1-A1 的 planning contract 是之後 integration / persistence 工作包的
# source of truth，只存在本機的話，下一包會照著一份沒有人看得到的文件做。
!docs/CHILD_GOAL_PLANNING_CONTRACT.md

# AI tooling (local only)
.agents/
.superpowers/
skills-lock.json
/.codegraph


.tmp-expo-web*

# Supabase CLI 的本機連線狀態（project-ref / pooler-url / 各服務版本）。
# 這是「這台機器現在連哪一個 project」，不是專案設定 —— 由 Git 決定的話，
# 任何人 link 到自己的 staging 都會產生 diff，也容易把別人的目標帶進 PR。
supabase/.temp/
